# helperis-beta production snapshot

This folder is a static snapshot of https://helperis-beta.vercel.app captured on 2026-05-09.

It is kept to compare the production deployment against the local source tree. It is not a deployable source checkout.

Files:

- `index.html`, `dashboard.html`, `admin.html`: captured HTML responses.
- `assets/`: captured Next.js static chunks referenced by the pages.
- `api-status.md`: API endpoint status observed during capture.
